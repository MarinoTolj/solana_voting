/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findPollPda } from "../pdas";
import { VOTING_PROGRAM_ADDRESS } from "../programs";
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CLOSE_POLL_DISCRIMINATOR = new Uint8Array([
  139, 213, 162, 65, 172, 150, 123, 67,
]);

export function getClosePollDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CLOSE_POLL_DISCRIMINATOR);
}

export type ClosePollInstruction<
  TProgram extends string = typeof VOTING_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountPoll extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountPoll extends string
        ? WritableAccount<TAccountPoll>
        : TAccountPoll,
      ...TRemainingAccounts,
    ]
  >;

export type ClosePollInstructionData = {
  discriminator: ReadonlyUint8Array;
  pollId: bigint;
};

export type ClosePollInstructionDataArgs = { pollId: number | bigint };

export function getClosePollInstructionDataEncoder(): FixedSizeEncoder<ClosePollInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["pollId", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CLOSE_POLL_DISCRIMINATOR }),
  );
}

export function getClosePollInstructionDataDecoder(): FixedSizeDecoder<ClosePollInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["pollId", getU64Decoder()],
  ]);
}

export function getClosePollInstructionDataCodec(): FixedSizeCodec<
  ClosePollInstructionDataArgs,
  ClosePollInstructionData
> {
  return combineCodec(
    getClosePollInstructionDataEncoder(),
    getClosePollInstructionDataDecoder(),
  );
}

export type ClosePollAsyncInput<
  TAccountAuthority extends string = string,
  TAccountPoll extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  poll?: Address<TAccountPoll>;
  pollId: ClosePollInstructionDataArgs["pollId"];
};

export async function getClosePollInstructionAsync<
  TAccountAuthority extends string,
  TAccountPoll extends string,
  TProgramAddress extends Address = typeof VOTING_PROGRAM_ADDRESS,
>(
  input: ClosePollAsyncInput<TAccountAuthority, TAccountPoll>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ClosePollInstruction<TProgramAddress, TAccountAuthority, TAccountPoll>
> {
  // Program address.
  const programAddress = config?.programAddress ?? VOTING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    poll: { value: input.poll ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.poll.value) {
    accounts.poll.value = await findPollPda({
      pollId: expectSome(args.pollId),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.poll),
    ],
    data: getClosePollInstructionDataEncoder().encode(
      args as ClosePollInstructionDataArgs,
    ),
    programAddress,
  } as ClosePollInstruction<TProgramAddress, TAccountAuthority, TAccountPoll>);
}

export type ClosePollInput<
  TAccountAuthority extends string = string,
  TAccountPoll extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  poll: Address<TAccountPoll>;
  pollId: ClosePollInstructionDataArgs["pollId"];
};

export function getClosePollInstruction<
  TAccountAuthority extends string,
  TAccountPoll extends string,
  TProgramAddress extends Address = typeof VOTING_PROGRAM_ADDRESS,
>(
  input: ClosePollInput<TAccountAuthority, TAccountPoll>,
  config?: { programAddress?: TProgramAddress },
): ClosePollInstruction<TProgramAddress, TAccountAuthority, TAccountPoll> {
  // Program address.
  const programAddress = config?.programAddress ?? VOTING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    poll: { value: input.poll ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.poll),
    ],
    data: getClosePollInstructionDataEncoder().encode(
      args as ClosePollInstructionDataArgs,
    ),
    programAddress,
  } as ClosePollInstruction<TProgramAddress, TAccountAuthority, TAccountPoll>);
}

export type ParsedClosePollInstruction<
  TProgram extends string = typeof VOTING_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    poll: TAccountMetas[1];
  };
  data: ClosePollInstructionData;
};

export function parseClosePollInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedClosePollInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), poll: getNextAccount() },
    data: getClosePollInstructionDataDecoder().decode(instruction.data),
  };
}
