/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findPollPda } from "../pdas";
import { VOTING_PROGRAM_ADDRESS } from "../programs";
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const END_POLL_DISCRIMINATOR = new Uint8Array([
  203, 232, 109, 145, 93, 37, 203, 68,
]);

export function getEndPollDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(END_POLL_DISCRIMINATOR);
}

export type EndPollInstruction<
  TProgram extends string = typeof VOTING_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountPoll extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountPoll extends string
        ? WritableAccount<TAccountPoll>
        : TAccountPoll,
      ...TRemainingAccounts,
    ]
  >;

export type EndPollInstructionData = {
  discriminator: ReadonlyUint8Array;
  pollId: bigint;
};

export type EndPollInstructionDataArgs = { pollId: number | bigint };

export function getEndPollInstructionDataEncoder(): FixedSizeEncoder<EndPollInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["pollId", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: END_POLL_DISCRIMINATOR }),
  );
}

export function getEndPollInstructionDataDecoder(): FixedSizeDecoder<EndPollInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["pollId", getU64Decoder()],
  ]);
}

export function getEndPollInstructionDataCodec(): FixedSizeCodec<
  EndPollInstructionDataArgs,
  EndPollInstructionData
> {
  return combineCodec(
    getEndPollInstructionDataEncoder(),
    getEndPollInstructionDataDecoder(),
  );
}

export type EndPollAsyncInput<
  TAccountAuthority extends string = string,
  TAccountPoll extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  poll?: Address<TAccountPoll>;
  pollId: EndPollInstructionDataArgs["pollId"];
};

export async function getEndPollInstructionAsync<
  TAccountAuthority extends string,
  TAccountPoll extends string,
  TProgramAddress extends Address = typeof VOTING_PROGRAM_ADDRESS,
>(
  input: EndPollAsyncInput<TAccountAuthority, TAccountPoll>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  EndPollInstruction<TProgramAddress, TAccountAuthority, TAccountPoll>
> {
  // Program address.
  const programAddress = config?.programAddress ?? VOTING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    poll: { value: input.poll ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.poll.value) {
    accounts.poll.value = await findPollPda({
      pollId: expectSome(args.pollId),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.poll),
    ],
    data: getEndPollInstructionDataEncoder().encode(
      args as EndPollInstructionDataArgs,
    ),
    programAddress,
  } as EndPollInstruction<TProgramAddress, TAccountAuthority, TAccountPoll>);
}

export type EndPollInput<
  TAccountAuthority extends string = string,
  TAccountPoll extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  poll: Address<TAccountPoll>;
  pollId: EndPollInstructionDataArgs["pollId"];
};

export function getEndPollInstruction<
  TAccountAuthority extends string,
  TAccountPoll extends string,
  TProgramAddress extends Address = typeof VOTING_PROGRAM_ADDRESS,
>(
  input: EndPollInput<TAccountAuthority, TAccountPoll>,
  config?: { programAddress?: TProgramAddress },
): EndPollInstruction<TProgramAddress, TAccountAuthority, TAccountPoll> {
  // Program address.
  const programAddress = config?.programAddress ?? VOTING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    poll: { value: input.poll ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.poll),
    ],
    data: getEndPollInstructionDataEncoder().encode(
      args as EndPollInstructionDataArgs,
    ),
    programAddress,
  } as EndPollInstruction<TProgramAddress, TAccountAuthority, TAccountPoll>);
}

export type ParsedEndPollInstruction<
  TProgram extends string = typeof VOTING_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    poll: TAccountMetas[1];
  };
  data: EndPollInstructionData;
};

export function parseEndPollInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedEndPollInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), poll: getNextAccount() },
    data: getEndPollInstructionDataDecoder().decode(instruction.data),
  };
}
